# Generated by stoatworks-unraid/scripts/gen-docker.mjs from fleet.json.
# Repo: birddog-play-flasher. Edit fleet.json and regenerate — direct edits are overwritten.
#
# Pull the published image by default. Uncomment build: . to build from this
# checkout instead — useful when testing a change before it reaches GHCR.

services:
  birddog-play-flasher:
    image: ghcr.io/stoatworks-labs/birddog-play-flasher:latest
    # build: .
    container_name: birddog-play-flasher
    restart: unless-stopped
    ports:
      - "8539:80"
